# Requirement-Tree Freeze Record

Requirement tree: **002-requirements v1.0.0**
Frozen on: **2026-08-15**
Decision: **Approved and frozen for atomic requirement derivation**

The frozen tree contains 66 requirement families. New evidence may refine atomic requirements inside an existing family. Adding/removing/renaming a family or changing a conceptual dependency requires a versioned amendment to this package.
